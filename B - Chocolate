#include <stdio.h>

int main(void){
  int N, d, x, t, c, total_c;
  scanf("%d %d %d", &N, &d, &x);
  total_c = x;
  for(int i=0; i<N; i++){
    scanf("%d", &t);
    c = ((d-1)/t) + 1;
    total_c += c;
  }
  printf("%d", total_c);
  return 0;
}
