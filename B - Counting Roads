#include <stdio.h>

int main(void){
  int N, M, t_1, t_2, n[55]={0};
  scanf("%d %d", &N, &M);
  for(int i=0; i<M; i++){
    scanf("%d %d", &t_1, &t_2);
    n[t_1]++;
    n[t_2]++;
  }
  for(int i=1; i<=N; i++){
    printf("%d\n", n[i]);
  }
  return 0;
}
