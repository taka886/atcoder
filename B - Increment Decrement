#include <stdio.h>

int main(void){
  int x=0, N, max=0;
  char s[105];
  
  scanf("%d %s", &N, s);
  for(int i=0; i<N; i++){
    if(s[i]=='D'){
      x--;
    }
    else{
      x++;
      if(max < x) max = x;
    }
  }
  printf("%d", max);
  return 0;
}
