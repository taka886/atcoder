#include <stdio.h>

int main(void){
  long long N, t, num_down=0, min;
  scanf("%lld", &N);
  min = N + 1;
  for(int i=0; i<N; i++){
    scanf("%lld", &t);
    if(min > t){
      num_down++;
      min = t;
    }  
  }
  printf("%lld", num_down);
  return 0;
}
