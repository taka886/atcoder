#include <stdio.h>

int main(void){
  long long b, f_b=1000000, sum=0;
  int N;
  scanf("%d", &N);
  for(int i=0; i<N-1; i++){
    scanf("%lld", &b);
    if(f_b >= b) sum += b;
    else sum += f_b;
    f_b = b;
  }
  sum += b;
  printf("%lld", sum);
  return 0;
}
