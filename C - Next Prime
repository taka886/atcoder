#include <stdio.h>

int is_prime(int a){
  for(int i=2; i*i<=a; i++){
    if(a%i==0) return 0;
  }
  return 1;
}

int main(void){
  int x, p=0;
  scanf("%d", &x);
  
  while(p==0){
    p = is_prime(x);
    x++;
  }
  printf("%d", x-1);
  return 0;
}
