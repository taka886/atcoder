#include <stdio.h>

int main(void){
  int a, a_2, b, b_2, c;
  scanf("%d %d", &a, &b);
  a_2 = a;
  b_2 = b;
  a = (int)(a / 0.08);
  b = (int)(b / 0.1);
  c = a < b ? a : b;
  
  for(int i=c; i<c + 13; i++){
    if((int)(i * 0.08) == a_2 && (int)(i * 0.1) == b_2){
      printf("%d", i);
      return 0;
    }
  }
  printf("%d", -1);
  return 0;
}
